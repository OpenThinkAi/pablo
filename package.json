{
  "name": "@openthink/pablo-root",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "test:tray": "bun test packages/cli/test/tray-helper.test.ts",
    "pablo": "bun run packages/cli/src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.0"
  },
  "//trustedDependencies": "@openthink/ui-leaf's postinstall downloads and sha256-verifies the platform ui-leaf-bin. Bun blocks postinstalls by default, and without this the binary pablo edit's window needs is simply absent.",
  "trustedDependencies": [
    "@openthink/ui-leaf"
  ]
}
